{
  "name": "video-production-framework",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "workspaces": ["packages/*"],
  "scripts": {
    "build": "npm run build --workspace @vpf/domain && npm run build --workspace @vpf/workflow && npm run build --workspace @vpf/production-system && npm run build --workspace @vpf/story && npm run build --workspace @vpf/storage",
    "typecheck": "npm run build --workspace @vpf/domain && npm run build --workspace @vpf/production-system && npm run build --workspace @vpf/story && npm run typecheck --workspace @vpf/workflow && npm run typecheck --workspace @vpf/storage",
    "test": "npm run build --workspace @vpf/domain && npm run build --workspace @vpf/production-system && npm run build --workspace @vpf/story && npm run build --workspace @vpf/storage && npm run test --workspace @vpf/story && npm run test --workspace @vpf/storage"
  },
  "engines": {"node": ">=22"},
  "devDependencies": {
    "@types/node": "^22.15.0",
    "tsx": "^4.19.4",
    "typescript": "^5.8.3"
  }
}
